# Docker environment variables
# Used when building for Docker container

# API Base URL - use relative path for Nginx proxy
VITE_API_BASE_URL=/api/v1

# API Timeout (milliseconds)
VITE_API_TIMEOUT=30000
